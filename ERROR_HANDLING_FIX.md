# Исправление: Обработка ошибок валидации от бекэнда

## ❌ Проблема
Сообщения об ошибках валидации от ASP.NET Core не отображались пользователю.

Пример ответа от бекэнда:
```json
{
  "errors": {
    "Reason": ["Причина должна быть от 20 до 2000 символов"]
  }
}
```

## ✅ Решение

### 1. Добавлена функция `extractErrorMessage()`

Эта функция умеет обрабатывать различные форматы ошибок от ASP.NET Core:

#### Формат 1: ModelState errors
```json
{
  "errors": {
    "Reason": ["Причина должна быть от 20 до 2000 символов"],
    "PlayerNick": ["Игровой ник обязателен"]
  }
}
```
**Результат:** "Причина должна быть от 20 до 2000 символов; Игровой ник обязателен"

#### Формат 2: Простое сообщение
```json
{
  "success": false,
  "message": "Слишком много запросов. Подождите минуту."
}
```
**Результат:** "Слишком много запросов. Подождите минуту."

#### Формат 3: ProblemDetails
```json
{
  "type": "https://tools.ietf.org/html/rfc7231#section-6.5.1",
  "title": "One or more validation errors occurred.",
  "status": 400,
  "errors": {
    "Discord": ["Укажите Discord"]
  }
}
```
**Результат:** "Укажите Discord"

### 2. Обновлены функции отправки

- `sendFeedbackToBackend()` - теперь использует `extractErrorMessage()`
- `sendTeamApplicationToBackend()` - теперь использует `extractErrorMessage()`

### 3. Добавлено логирование

Все ошибки от бекэнда логируются в консоль браузера:
```javascript
console.error('Backend error response:', result);
```

## 🧪 Тестирование

### Способ 1: Тестовая страница
Откройте `test-error-parsing.html` в браузере и нажмите кнопки "Тест" для каждого формата ошибки.

### Способ 2: Реальная форма
1. Откройте `shop.html`
2. Нажмите "Обратная связь" или "Вступить в команду"
3. Заполните форму неправильно (например, слишком короткое сообщение)
4. Нажмите "Отправить"
5. Откройте DevTools (F12) → Console - там будет полный ответ от сервера
6. На экране появится красное сообщение об ошибке

## 📋 Примеры ошибок, которые теперь видны

### Валидация полей
- ✅ "Игровой ник обязателен"
- ✅ "Ник должен быть от 2 до 50 символов"
- ✅ "Сообщение должно быть от 10 до 2000 символов"
- ✅ "Причина должна быть от 20 до 2000 символов"
- ✅ "Укажите контактные данные"

### Rate Limiting
- ✅ "Слишком много запросов. Подождите минуту."
- ✅ "Превышен лимит запросов в час. Попробуйте позже."

### Системные ошибки
- ✅ "Настройки Telegram не сконфигурированы"
- ✅ "Ошибка при отправке в Telegram"

## 🔍 Отладка

Если сообщение об ошибке не отображается:

1. **Откройте DevTools** (F12)
2. **Перейдите на вкладку Console**
3. **Попробуйте отправить форму**
4. **Найдите строку** `Backend error response:`
5. **Посмотрите структуру ответа** - возможно, формат отличается

Если формат отличается, добавьте обработку в функцию `extractErrorMessage()`.

## 📝 Изменённые файлы

- ✅ `script.js` - добавлена функция `extractErrorMessage()` и логирование
- ✅ `styles.css` - стили для `.form-message.error` и `.form-message.success`
- ✅ `test-error-parsing.html` - тестовая страница для проверки парсинга ошибок
